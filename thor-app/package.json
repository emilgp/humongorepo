{
  "name": "thor-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "^3.0.1",
    "@herogearstuffs/mjolnir": "1.0.0",
    "@herogearstuffs/stormbreaker": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "publish:dev": "amplify env checkout dev && REACT_APP_STAGE=dev amplify publish",
    "publish:test": "amplify env checkout test && REACT_APP_STAGE=test amplify publish",
    "publish:release": "amplify env checkout release && REACT_APP_STAGE=release amplify publish"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
